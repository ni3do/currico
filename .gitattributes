# Auto detect text files and normalise to LF in the repo
* text=auto eol=lf

# Ensure shell scripts always use LF (critical for Docker containers)
*.sh text eol=lf

# Keep Windows batch files with CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
